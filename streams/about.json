{
    "config": {
        "source": {
            "format": "json"
        }
    },
    "fields": {
        "sort": "integer",
        "title": "string",
        "details": "array",
        "enabled": "boolean"
    }
}
